apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aut-pvc
spec:
  storageClassName: local-storage
  volumeName: aut-pv
  accessModes: 
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi