apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: acd-pvc
spec:
  storageClassName: local-storage
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi