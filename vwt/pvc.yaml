apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vwt-pvc
spec:
  storageClassName: local-storage
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi