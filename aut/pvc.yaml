apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aut-pvc
spec:
  storageClassName: local-storage
  accessModes: 
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi